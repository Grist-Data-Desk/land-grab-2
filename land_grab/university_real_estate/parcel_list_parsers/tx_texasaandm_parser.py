def txam_parser(l):
  # could combine into one doc, but there are two.
  # for property holdings, don't include any with text string. just take out - and concatenate
    # column 1 or [0]
    # ignore rows 1-8 (string descriptions)
  # for surface+mineral, ignore blanks and ones that say "Parcel Number", take out spaces and - and concatenate
    # column 3 or [2]

  pass